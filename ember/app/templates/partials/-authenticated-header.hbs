<header>
  <div id="topbar" class="clearfix">
    <a class="search-button" {{action "toggleSearch"}}>
      <i class="icon-search"></i>
    </a>

    <div class="logo">
      <h1>
        {{#link-to 'contacts' class="title"}}
          Grezha
        {{/link-to}}
      </h1>
    </div>

    <a class="menu-button" {{action "toggleMenu"}}>
      <i class="icon-menu"></i>
    </a>

    <nav {{bind-attr class="isMenuShowing:menu:desktop-only"}}>
      <ul class="links">
        <li>
          {{#link-to 'contacts' id="contacts-link"}}
            Clients ({{allContacts.length}})
          {{/link-to}}
        </li>

        {{#toggled-feature name='vehicles' tagName='li'}}
          {{#link-to 'vehicles'}} Vehicles {{/link-to}}
        {{/toggled-feature}}

        {{#if apiConnection.online}}
          <li>{{#link-to 'birthdays'}}Birthdays{{/link-to}}</li>
        {{/if}}
      </ul>

      <div class="mobile-only">
        <div class="account-info">
          {{session.username}} - {{session.organization}}
        </div>

        <ul class="account-links">
          <li class="reset-password">
            {{#link-to 'reset-password' 'current-user'}}
              Reset Password
            {{/link-to}}
          </li>
          <li>
            <a href {{action "invalidateSession"}} id="logout">
              Logout
            </a>
          </li>
        </ul>
      </div>

      <div class="login-info">
        {{#unless apiConnection.online}}
          <div class="offline">
            Offline
          </div>
        {{/unless}}

        {{#unless isProduction}}
          <div class="environment">
            {{environment}}
          </div>
        {{/unless}}

        <div class="organization">
          {{#if session.admin}}
            <a target="_blank" href="{{unbound adminURL}}">
              {{session.organization}}
              <i class="icon-cog"></i>
            </a>
          {{else}}
            {{session.organization}}
          {{/if}}
        </div>

        <div {{bind-attr class=":dropdown apiConnection.online:online:offline"}}>
          <a data-toggle="dropdown" id href="#">
            <span id="username">{{session.username}}</span>
            <span class="caret"></span>
          </a>

          <ul class="dropdown-menu dropdown-menu-right" role="menu">
            <li class="reset-password">
              {{#link-to 'reset-password' 'current-user'}}
                Reset Password
              {{/link-to}}
            </li>
            <li class="divider"></li>
            <li>
              <a href {{action "invalidateSession"}} id="logout">
                Logout
              </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </div>
</header>