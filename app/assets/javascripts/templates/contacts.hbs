<div id="sidebar">
  <ul class="contacts">
    <li class="new-contact">
      {{#link-to 'contacts.new'}}
        Add Contact
      {{/link-to}}
    </li>

    {{#each}}
      <li class="contact">
        {{#link-to 'contact' this}}
          {{name}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</div>

<div id="main-panel">
  {{outlet}}
</div>
